napari-meisenmeister is a napari widget for bilateral breast MRI classification with MeisenMeister.

It lets users map three loaded image layers to `pre`, `post1`, and `post2`, then runs MeisenMeister portable inference with `fold_all`, generates breast-side masks under the hood, and presents left/right probability cards directly inside the dock widget.
